// @ts-check
/**
 * Additionne 2 nombre
 * @param {number} a Le premier nombre
 * @param {number} b Le 2e nombre
 * @returns {number} La somme
 */
const sum = (a, b) => a + b;

for (let i = 0; i < 10; i++) {
  console.log(sum(i, i));
}
